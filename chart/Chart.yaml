apiVersion: v2
name: portainer-run
description: Portainer-Run — AI-assisted deployment companion for Portainer, served as a Portainer addon.
type: application

# version and appVersion are both set to the release version at publish time
# (see docs/releases.md) — the values below are placeholders, bumped here to
# match the actual current latest release so the now-continuous dev chart
# publishing doesn't claim a version behind the real product.
version: 1.3.3
appVersion: '1.3.3'
